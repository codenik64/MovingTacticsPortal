@model IEnumerable<MovingTactics.Portal.Models.ViewModels.SurveyResponseViewModel>

<h2>Survey Response Summary</h2>

<br/>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Survey)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Member)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.DateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeCreated)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Survey)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Member)
    </td>
    @*<td>
        @Html.DisplayFor(modelItem => item.DateCreated)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TimeCreated)
    </td>*@
    <td>
        @Html.ActionLink("Response Summary", "ResponseSummary", new { id = item.SurveyResponseId },new { @class = "btn btn-outline-dark"})
    </td>

   

</tr>
}

</table>
